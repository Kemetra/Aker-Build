{
  "name": "@aker-build/review",
  "version": "0.0.0",
  "description": "Aker Build PR Reviewer — review a local diff or GitHub PR against the SaaS gates and declared scope, returning a Ready / Not Ready / Needs Verification verdict",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aker-build/config": "workspace:*",
    "@aker-build/gates": "workspace:*",
    "@aker-build/project-map": "workspace:*",
    "@aker-build/queue": "workspace:*",
    "@aker-build/scanner": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}

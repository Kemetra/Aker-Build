{
  "name": "@aker-build/eval",
  "version": "0.0.0",
  "description": "Aker Build detection-quality benchmark harness (precision/recall per confidence tier)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "aker-build-benchmark": "./src/bin.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aker-build/gates": "workspace:*",
    "@aker-build/project-map": "workspace:*",
    "@aker-build/scanner": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}

{
  "name": "@aker-build/prompt",
  "version": "0.0.0",
  "description": "Aker Build Agent Prompt Compiler — compile safe, scoped agent prompts from a routed queue item",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aker-build/queue": "workspace:*",
    "@aker-build/scanner": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
